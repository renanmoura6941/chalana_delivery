import 'package:flutter/material.dart';

const ERRO_SEM_FOTO = Center(
    child: Icon(
  Icons.photo,
  size: 100,
  color: Colors.blue
  
));

const IMAGEM_VAZIA = [
  Icon(
    Icons.photo,
    size: 100,
  )
];
